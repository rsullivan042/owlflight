<nav class="bg-black min-w-full flex justify-center gap-8 border-b border-stone-800 pb-4">
  <% admin_navbar_links.each do |link| %>
    <%= link_to link[:text], link[:path], class: nav_link_classes(link[:path]) %>
  <% end %>
</nav>
