<div class="bg-black min-w-full flex justify-center border-b border-stone-800 pb-4">
  <% navbar_links.each do |button| %>
    <button>
      <%= link_to button[:text], button[:path], class: nav_link_classes(button[:path]) %>
    </button>
  <% end %>
</div>
